# Included by recipe generated by superflore

# In general, ament things create non-symlink .so file but this is not.
FILES_${PN}_remove = "${libdir}/lib*.so"
FILES_${PN}_append = " ${bindir} ${libdir}/lib*.so.*"
FILES_${PN}-dev_append = " ${libdir}/lib*.so"

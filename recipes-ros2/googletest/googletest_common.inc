# Included by recipe generated by superflore

BBCLASSEXTEND = "native"

do_install_append() {
    rm -rf ${D}${prefix}/src
}

FILES_${PN}_prepend = "${datadir}/${ROS_BPN}/package.xml "
FILES_${PN}-dev_prepend = "${datadir}/${ROS_BPN}/cmake "

# Included by recipe generated by superflore

VENDOR_DIR = "${datadir}/ros1_rosbag_storage"

do_install_append() {
    rm -f ${D}${VENDOR_DIR}/local_setup.*
    rm -rf ${D}${VENDOR_DIR}/environment
}

FILES_${PN}_prepend = " \
    ${VENDOR_DIR}/package.xml \
"

FILES_${PN}-dev_prepend = " \
    ${VENDOR_DIR}/cmake \
"

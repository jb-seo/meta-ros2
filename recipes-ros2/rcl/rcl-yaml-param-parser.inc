# Included by recipe generated by superflore

# libyaml appears in both ROS_BUILD_DEPENDS and ROS_EXPORT_DEPENDS, so it's easier to remove it from DEPENDS.
DEPENDS_remove = "libyaml"
ROS_EXEC_DEPENDS_remove = "libyaml"

ROS_BUILD_DEPENDS += "libyaml-vendor"
ROS_EXEC_DEPENDS += "libyaml-vendor"

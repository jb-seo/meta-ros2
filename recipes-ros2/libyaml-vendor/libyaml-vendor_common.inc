# Included by recipe generated by superflore

DEPENDS_append = " libyaml"

do_install_append() {
    rm -rf ${D}${includedir}
    rm -rf ${D}${libdir}
}

FILES_${PN}-dev_append = " ${prefix}/cmake"
SYSROOT_DIRS_append = " ${prefix}/cmake"
